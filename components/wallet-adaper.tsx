'use client'

interface WalletModalProps {
  children: React.ReactNode
}

export default function WalletAdaper({ children }: WalletModalProps) {
  // Declare network

  // Serialize the network to get endpoint

  // Define list of wallet provider

  return <>{children}</>
}
